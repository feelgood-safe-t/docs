# 채점 가이던스 — 필요 여부 판단

> 작성 2026-09-04 · **개정 2026-09-04** (제품 명세 `09b73cc` 확인 후)
> **배경:** 팀 결정은 *"채점 기준을 따로 정의하지 않고 LLM에 결과 데이터만 제공해 일임"*이었다.
> 확인 결과 **제품 명세에 배점표와 채점 규칙이 이미 정의되어 있어**, 이 문서의 초판 전제 대부분이 해소됐다.

---

## 0. 결론 — 추가로 만들 것은 거의 없다

`spec/README.md` §과정 평가가 다음을 이미 정의한다.

| 항목 | 명세의 정의 |
|---|---|
| **배점표** | 7개 항목 100점 (문항당) |
| **총점·통과** | 세 문항 평균, 반올림 전 **70 이상 PASS** |
| **미응답 처리** | 문항 점수 **정확히 0**, **결정론적 규칙**으로 적용 |
| **조기 종료** | 문항 완료·시험 종료 **모두 고정 감점 없음** |
| **역할 분리** | 항목 점수는 **LLM**, 범위 검증·합계·평균·PASS는 **규칙 코드** |
| **환각 방지** | LLM 출력이 누락·범위 초과면 **임의 보정 없이 실패 거부** 후 버전 기록 재시도 |
| **재현성** | `(snapshotHash, rubricVersion, promptVersion, modelVersion)` 동일 재요청은 **저장된 동일 결과 반환** |

> **초판에서 제기한 위험 4가지가 모두 해소됐다.**
> 배점표 미준수(R2)·재현성(R3)·설명가능성(R4)은 명세와 `rubricVersion` 관리로, 관찰 불가 항목의 환각(R1)은
> **배점표가 전부 관찰 가능한 행동 항목으로 재설계**되면서 해결됐다.
>
> **결론: 채점 기준을 새로 작성할 필요가 없다.** 현재 배점표를 버전으로 고정해 다루는 것으로 충분하다.

---

## 1. 개정된 배점표 — 무엇이 바뀌었나

| 이전(초기) 배점표 | 개정 배점표 |
|---|---|
| 위험 한도 준수 20 · 레버리지 통제 20 · 자산 집중도 10 | **삭제** |
| 정보 출처 검증 15 | → 정보 활용과 출처 구분 15 |
| 손실 이후 행동 10 · 판단의 일관성 10 | → 판단·확신도·근거의 일관성 10 |
| 전망 및 확신도 5 · 기초 개념 퀴즈 5 · 판단 근거 구체성 5 | → 판단 근거의 유용성 10 |
| — | **신설** 위험 신호 인식과 대응 20 · 불확실성과 변동성 인식 15 · 시장 흐름 해석 15 · **초기 성향과 응답의 정합성 15** |
| 전체 100점 중 70점 통과 | **문항당 100점 → 3문항 평균 70점 통과** |

**의미 있는 변화 세 가지**

1. **측정 불가 항목이 전부 사라졌다.** 포지션·수익률·집중도·레버리지 상품에 의존하던 50점이 제거되고, 그 자리를 *위험 신호 인식*, *불확실성 인식*, *시장 흐름 해석* 같은 **관찰 가능한 판단 행동**이 채웠다.
2. **설문이 채점에 정식으로 들어왔다.** 「초기 성향과 응답의 정합성」 **15점**이다. 앞서 이 문서와 설문 명세에서 *"온보딩은 시나리오 매칭용이며 채점 근거가 아니다"*라고 정리했는데, **개정 명세는 설문 응답과 실제 판단의 정합을 직접 채점한다.**
3. **확신도가 필수 입력으로 돌아왔다.** 배점 두 항목(불확실성 인식 15, 일관성 10)이 확신도를 증거로 쓴다.

---

## 2. ✅ "고정 rubric"의 실체 — 현재 배점표가 곧 rubric이다

명세는 *"`ANSWERED`: **고정 rubric**에 따라 LLM이 항목별 점수와 근거를 구조화해 반환"*이라 쓴다. 이 rubric이 별도 산출물인지 확인한 결과, **배점표 그 자체**다.

### 근거 — `spec/README.md` 내 rubric 언급 6곳

| 위치 | 내용 | 시사점 |
|---|---|---|
| L288 | *"**고정 rubric**에 따라 LLM이 항목별 점수와 근거를 반환"* | "고정 rubric"이라는 표현이 등장하는 **유일한 곳** |
| **L230** | *"공개된 `rubricVersion`의 **해당 과정 평가 항목**에서 활용할 증거가 적을 수 있다"* | ⭐ **결정적** — rubric의 내용물을 **"과정 평가 항목"**이라고 지칭한다. 즉 §과정 평가의 배점표가 rubric이다 |
| L398 | 포지션·거래 고도화 WIP에는 **별도** 평가 rubric이 필요 | 현재 PoC용 rubric은 **이미 존재**한다는 전제 |
| L277 | Evaluation Service가 `snapshotHash`와 함께 rubric·prompt·model 버전을 별도 저장 | 저장·버전 관리 대상 |
| L320 | `(snapshotHash, rubricVersion, promptVersion, modelVersion)` 동일 재요청 → 저장된 동일 결과 | 재현성 캐싱 키 |
| L404 | 필수 비기능 조건 — *"평가 rubric은 immutable version으로 관리"* | 불변 버전 관리 요구 |

> **별도 rubric 문서를 작성할 필요가 없다.** 팀 결정("가이던스를 따로 만들지 않는다")과 명세는 충돌하지 않는다.
> 명세가 요구하는 것은 *새 기준을 쓰는 것*이 아니라 **이미 있는 배점표를 버전으로 고정해 다루는 것**이다.

### 그래서 실제로 필요한 것 — 3가지

| # | 항목 | 근거 | 성격 |
|---|---|---|---|
| **1** | 배점표에 **버전 식별자 부여** (예: `safe-t-rubric/1.0`)하고 변경 시 새 버전 발급 | L404 *"immutable version으로 관리"* | 엔진 담당 |
| **2** | 평가 결과에 **`rubricVersion` 기록** | L320 · L412 | 엔진 담당 |
| **3** | 채점 기준의 **공개** | L230 *"**공개된** `rubricVersion`"* | 🔴 **기획·프론트 범위** — 아래 참조 |

### 3번 — 채점 기준을 사용자에게 보여주는 것

명세는 `rubricVersion`을 **"공개된"**이라고 수식한다. 채점 기준이 사용자에게 열려 있어야 한다는 뜻이다.

결과 보고서에 항목별 점수가 표시되므로 사후에는 부분적으로 충족되지만, **평가 전에 "무엇을 어떻게 채점하는지"를 안내**하면 명세 요구에 정확히 맞는다.

**왜 그게 더 나은가**
- **교육 효과** — *"수익률이 아니라 판단 과정을 본다"*를 미리 알아야 행동이 달라진다. 모르고 하면 여전히 방향 맞히기 게임이 된다
- **설명가능성** — 주최가 금융보안원이다. 채점 기준이 사전 공개되어 있다는 사실 자체가 *"AI가 임의로 점수를 매긴다"*는 의심을 차단한다
- **비용** — 배점표를 그대로 보여주는 안내 화면 하나면 된다

> 배치 제안: 온보딩 직후 또는 문항 시작 전 브리핑 화면. *"이렇게 채점됩니다"* + 7개 항목과 배점.

---

## 2-1. ⏸️ 보류 — 증거 부족 시 처리

명세는 *"답변한 item이라도 관찰 가능한 행동 이력이 적으면 활용할 증거가 적을 수 있다"*고만 서술하고 점수 처리 방식은 정하지 않는다.

> **2026-09-04 결정 — 지금은 다루지 않는다.**
> 실제로 증거가 부족한 사례가 관찰되면 그때 처리 방안을 검토한다.
> 개정 배점표가 전부 관찰 가능한 행동 항목으로 구성되어 있어 발생 빈도가 낮을 것으로 보고, 선제적 규칙을 만들지 않는다.

---

## 3. 제출 서류에는 어떻게 쓰는가

기능명세서 **4번(AI 및 데이터 처리 방식)** 초안이다.

> 평가는 봉인된 판단 기록을 입력으로, 고정된 배점표에 따라 LLM이 항목별 점수와 근거를 생성한다.
> 총점 합산·평균·통과 판정과 미응답 문항의 0점 적용은 결정적 규칙 코드가 수행하며, LLM 출력이 범위를 벗어나면 보정하지 않고 실패로 처리한다.
> 산출물에는 입력 Snapshot 해시와 rubric·프롬프트·모델 버전을 함께 기록해, 동일 입력에 대해 저장된 동일 결과를 반환한다.

> ⚠️ 양식은 *"미구현 또는 향후 구현 예정 기능은 제외"*를 명시한다. **실제 구현을 확인한 뒤 확정할 것.**

---

## 4. 관찰 가능한 신호 — 참고

프롬프트에 예시로 넣으면 채점 품질이 올라간다. **규칙이 아니라 예시**이므로 LLM의 판단 여지는 유지된다.

| 신호 | 어디서 나오는가 |
|---|---|
| 판단 직전 열람한 정보의 **출처 유형** | 콘텐츠 열람 기록 |
| 열람 → 판단까지의 **간격** | 열람·판단 타임스탬프 |
| **아무것도 열람하지 않은 채** 내린 판단 비율 | 동일 |
| 판단 **번복 횟수와 시점** | 판단 기록 |
| 급변 이벤트 **직후의 반응** | 시장 타임라인 × 판단 시각 |
| 자기신고 **근거 태그와 실제 열람의 불일치** | 근거 입력 × 열람 기록 |
| 온보딩 **선언과 실제 행동의 괴리** | Snapshot에 함께 봉인된 설문 응답 |

> **정정** — 설문 응답은 개정 명세에서 **채점 항목**이다 (「초기 성향과 응답의 정합성」 15점).
> 다만 **중앙 엔진은 여전히 설문을 해석하지 않는다.** 원본만 저장해 Snapshot에 봉인하고, 해석과 채점은 외부 평가 서비스가 수행한다.

---

## 5. 제출 서류에는 어떻게 쓰는가

기능명세서 **4번(AI 및 데이터 처리 방식)**에 들어갈 문장이다.

> 평가는 봉인된 판단 기록을 입력으로 LLM이 항목별 점수와 근거를 생성하고, 총점 합산과 통과 판정은 결정적 코드가 수행한다.
> 채점 프롬프트에는 평가 항목과 배점, 그리고 **관찰 근거가 없는 항목을 추정하지 않고 제외하는 규칙**이 포함된다.
> 산출물에는 프롬프트 버전·모델 버전·입력 해시를 함께 기록해 동일 입력에 대한 재현을 확인할 수 있다.

> ⚠️ **이 문단을 쓸 수 있으려면 (b)와 (c)가 실제로 구현돼 있어야 한다.**
> 구현되지 않은 것을 기능명세서에 쓰면 규정 위반이다 — 양식은 *"미구현 또는 향후 구현 예정 기능은 제외"*를 명시한다.

---

## 5. 팀에 전달할 사항

| # | 항목 | 대상 |
|---|---|---|
| 1 | 배점표에 **버전 식별자 부여**, 변경 시 새 버전 발급 | 엔진 담당 |
| 2 | 평가 결과에 **`rubricVersion` 기록** | 엔진 담당 |
| 3 | **채점 기준 사전 안내 화면** — 평가 전에 7개 항목과 배점을 보여준다 | 기획·프론트 |
| 4 | 채점 프롬프트에 **배점표를 그대로 포함** | 엔진 담당 |

> 나머지(총점·평균·PASS 코드 계산, 미응답 0점, 범위 초과 실패 처리, 재현성 캐싱)는
> **제품 명세에 이미 정의**되어 있으므로 별도 요청이 필요 없다.
>
> **새로 작성할 채점 기준 문서는 없다.**
